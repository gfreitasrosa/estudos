"""
Mapas em Python são conhecidos como dicionários

Identificado por chave e valor

# COMO ITERAR MAPAS / DICIONÁRIOS

    for chave in dicionario:
        print(chave) -> Imprime as chaves do dicionário

    for chave in dicionario:
        print(dicionario[chave]) -> Imprime os valores do dicionário

    for chave in receita:
        print(f"{chave} : {dicionario[chave]}) -> Imprime todas as chaves e os seus valores do dicionario

    for chave in receita.keys(): -> Modo pythonico
        print(chave)

    for valor in receita.values(): -> Modo pythonico
        print(valor)

# DESEMPACTAMENTO

    for chave, valores in receita.items():
        print(f"chave = {chave} valor = {valores})


# MÉTODOS 

    Soma*, Valor Máximo*, Valor mínimo*, Tamanho

    * = Apenas para valores inteiros ou reais





















"""